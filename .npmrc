# Allow legacy peer dependencies
legacy-peer-deps=true

# Use exact versions for reproducible builds
save-exact=false

# Faster installs
prefer-offline=true
audit=false
fund=false

# Suppress warnings
loglevel=error

# Avoid cache conflicts on Railway - use /tmp directory
cache=/tmp/.npm
modules-cache-max-age=0
